"use strict";

let age = "";

// do{
// const prompt = require("prompt-sync")();
// age = prompt("Enter your age: ");

// }while(age < 18);

while(age < 18){
    const prompt = require("prompt-sync")();
    age = prompt("Enter your age: ");
}